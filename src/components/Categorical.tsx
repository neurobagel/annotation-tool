import { Typography } from '@mui/material';

function Categorical() {
  return (
    <Typography variant="h6" data-cy="categorical-placeholder">
      Categorical Component
    </Typography>
  );
}

export default Categorical;
